@model Nutrition_App.Operations.Models.Log.EditLogViewModel

<h1>Edit Diet Log</h1>
<h3>@Model.Description</h3>

<h2>Portion Sizes</h2>
<table class="table table-striped table-bordered table-hover">
    <thead class="table-dark">
        <tr>
            <td>Description</td>
            <td>Weight</td>
        </tr>
    </thead>

    @for (int i = 0; i < Model.Food.FoodPortions.Count; i++)
    {
        @if (Model.Food.FoodPortions[i].PortionDescription == "Quantity not specified") continue;

        <tr>
            <td>@Model.Food.FoodPortions[i].PortionDescription</td>
            <td>@Model.Food.FoodPortions[i].GramWeight g</td>
        </tr>
    }
</table>

<div class="col-md-3">
    <form asp-action="Edit" method="post" class="mb-3">
        <div class="form-group">
            <label asp-for="WeightEaten">Amount Eaten (grams):</label>
            <input type="number" asp-for="WeightEaten" min="0" class="form-control" />
            <span asp-validation-for="WeightEaten" class="text-danger"></span>
        </div>

        <div class="form-group">
            <label asp-for="DateEaten">Date Eaten:</label>
            <input type="date" asp-for="DateEaten" class="form-control" />
            <span asp-validation-for="DateEaten" class="text-danger"></span>
        </div>

        <input type="hidden" asp-for="FoodId" />
        <input type="hidden" asp-for="DietLogId" />

        <button type="submit" class="btn btn-primary mt-2">Edit Log</button>
    </form>
</div>